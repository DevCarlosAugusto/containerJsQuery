{
  "name": "container_js_query",
  "version": "0.0.1",
  "description": "a javascript package to use css @container queries",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build": "tsup src/index.js --format cjs,esm --dts"
  },
  "author": "DevCarlosAugusto",
  "license": "MIT",
  "devDependencies": {
    "@vitest/browser": "^4.0.18",
    "jsdom": "^28.0.0",
    "tsup": "^8.5.1",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
